apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: risk-engine
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/acelehesap10-boop/K'
    targetRevision: HEAD
    path: infrastructure/helm/risk-engine
  destination:
    server: https://kubernetes.default.svc
    namespace: risk
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
