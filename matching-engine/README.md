# Matching Engine

This folder contains a prototype/poc matching engine in Rust.

How to run benchmarks:

1. Install Rust: https://rustup.rs
2. cd matching-engine
3. cargo bench

Docker: build with `cargo build --release` and packaging instructions can be added to `services/matching-engine/Dockerfile`.
