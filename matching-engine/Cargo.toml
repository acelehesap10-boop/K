[package]
name = "matching-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
criterion = "0.4"

[features]
dpdk = []
serde_json = "1.0"
anyhow = "1.0"
log = "0.4"
env_logger = "0.10"
 
[dev-dependencies]
criterion = "0.5"
